ame.gems.push({x:e.x+rand(-10,10),y:e.y+rand(-10,10),vx:rand(-45,45),vy:rand(-45,45),value:e.xp/gemCount,r:e.type==='boss'?5:4,color:e.type==='boss'?'#ffd166':'#6ee7ff'});if(Math.random()<(e.type==='boss'?1:.09)){game.coins++;sfx('coin');textPop(e.x,e.y-25,'◈ +1','#ffd166')}if(e.split)for(let i=0;i<2;i++){const scale=1+(game.wave-1)*.12;game.enemies.push({x:e.x+rand(-8,8),y:e.y+rand(-8,8),type:'mini',r:6,hp:14*scale,maxHp:14*scale,speed:100,damage:7,color:'#a7ffb7',xp:3,score:55,hit:0,orbHit:0,burnT:0,burnDps:0,poisonT:0,poisonDps:0,slowT:0,slowFactor:1})}if(p.deathBurst)areaDamage(e.x,e.y,78,p.damage*p.deathBurst,e,'#ff866f');if(e.type==='boss'){game.score+=5000;game.coins+=8;game.boss=null;toast('★ 領主已擊破！裂隙幣 +8');sfx('level')}burst(e.x,e.y,e.color,e.type==='boss'?45:14)}
function gainXp(v){const p=game.player;p.xp+=v;if(p.xp>=p.xpNeed){p.xp-=p.xpNeed;p.level++;p.xpNeed=Math.floor(p.xpNeed*1.28+8);showUpgrade()}}
function showUpgrade(){state='upgrade';sfx('level');const choices=[];let pool=[...upgradePool];while(choices.length<3&&pool.length){const weights=pool.map(u=>(u.weight??1)*(selectedHero==='mage'&&['史詩','傳說'].includes(u.rarity)?1.45:1)),total=weights.reduce((a,b)=>a+b,0);let r=Math.random()*total,idx=0;for(;idx<pool.length;idx++){r-=weights[idx];if(r<=0)break}choices.push(pool.splice(Math.min(idx,pool.length-1),1)[0])}$('upgradeCards').innerHTML='';for(const u of choices){const el=document.createElement('article');el.className='card upgrade-card '+(u.rarity==='史詩'?'epic':u.rarity==='傳說'?'legend':'');el.innerHTML=`<span class="icon">${u.rarity==='傳說'?'★':u.rarity==='史詩'?'✦':'◆'}</span><h3>${u.name}</h3><p>${u.desc}</p><span class="path">${u.path}流</span><span class="rarity">${u.rarity}</span>`;el.onclick=()=>{u.apply(game.player);$('upgradeOverlay').classList.remove('show');state='playing';last=performance.now();toast(u.name+' 已獲得！')};$('upgradeCards').appendChild(el)}$('upgradeOverlay').classList.add('show')}
function dash(){if(state!=='playing')return;const p=game.player;if(p.dashT>0)return;let x=p.dx,y=p.dy;if(Math.hypot(x,y)<.1){x=1;y=0}const l=Math.hypot(x,y)||1;p.dashVX=x/l*520;p.dashVY=y/l*520;p.dashing=.18;p.dashT=p.dashCooldown;if(p.dashDamage)areaDamage(p.x,p.y,120,p.damage*p.dashDamage,null,'#ff8bd0');sfx('dash');burst(p.x,p.y,p.color,12)}
function cycleFov(){save.fov=(save.fov+1)%FOV.length;persist();updateFovLabels();if(game){game.camera.zoom=FOV[save.fov].zoom;toast('視野：'+FOV[save.fov].name)}}
function update(dt){if(state!=='playing'||!game)return;dt=Math.min(dt,.033);const g=game,p=g.player;g.t+=dt;g.waveT+=dt;if(g.waveT>=g.waveLength){g.wave++;g.waveT=0;g.score+=g.wave*220;toast('WAVE '+g.wave);if(g.wave%5===0)spawnBoss()}g.spawnT-=dt;const targetCount=Math.min(90,8+g.wave*4);if(g.spawnT<=0&&g.enemies.length<targetCount){spawnEnemy();g.spawnT=Math.max(.1,.72-g.wave*.035)}let mx=(keys.ArrowRight||keys.d?1:0)-(keys.ArrowLeft||keys.a?1:0)+joystick.x,my=(keys.ArrowDown||keys.s?1:0)-(keys.ArrowUp||keys.w?1:0)+joystick.y;const ml=Math.hypot(mx,my);if(ml>1){mx/=ml;my/=ml}p.dx=mx;p.dy=my;p.invuln=Math.max(0,p.invuln-dt);p.dashT=Math.max(0,p.dashT-dt);p.orbitA+=dt*2.2;p.droneA-=dt*.8;if(p.regen)p.hp=Math.min(p.maxHp,p.hp+p.maxHp*p.regen*dt);if(p.maxShield){p.shieldDelay-=dt;if(p.shieldDelay<=0)p.shield=Math.min(p.maxShield,p.shield+p.maxShield*.16*dt)}if(p.dashing>0){p.dashing-=dt;p.x+=p.dashVX*dt;p.y+=p.dashVY*dt}else{p.x+=mx*p.speed*dt;p.y+=my*p.speed*dt}p.x=clamp(p.x,24,WORLD_W-24);p.y=clamp(p.y,24,WORLD_H-24);const rate=p.fireRate*(p.hp/p.maxHp<.4?1+p.berserk:1);p.shotT-=dt;if(p.shotT<=0&&g.enemies.length){firePlayer();p.shotT=1/rate}if(p.drones){p.droneT-=dt;if(p.droneT<=0){fireDrones();p.droneT=Math.max(.24,.78-p.drones*.07)}}if(p.nova){p.novaTimer-=dt;if(p.novaTimer<=0){p.novaTimer=p.nova;areaDamage(p.x,p.y,300,p.damage*1.15,null,'#c8b1ff');shake=8;sfx('boss')}}
for(const e of g.enemies){if(e.dead)continue;e.hit=Math.max(0,e.hit-dt);e.orbHit=Math.max(0,(e.orbHit||0)-dt);if(e.burnT>0){e.burnT-=dt;e.hp-=e.burnDps*dt;if(Math.random()<dt*5)burst(e.x,e.y,'#ff8a45',2)}if(e.poisonT>0){e.poisonT-=dt;e.hp-=e.poisonDps*dt;if(Math.random()<dt*4)burst(e.x,e.y,'#86ff71',1)}if(e.hp<=0){killEnemy(e);continue}if(e.slowT>0)e.slowT-=dt;else e.slowFactor=1;const a=Math.atan2(p.y-e.y,p.x-e.x),d=Math.hypot(p.x-e.x,p.y-e.y),ideal=e.shoot?190:e.type==='boss'?105:0,dir=d>ideal+15?1:e.shoot&&d<ideal-25?-1:0;e.x=clamp(e.x+Math.cos(a)*e.speed*e.slowFactor*dir*dt,20,WORLD_W-20);e.y=clamp(e.y+Math.sin(a)*e.speed*e.slowFactor*dir*dt,20,WORLD_H-20);if(e.shoot||e.type==='boss'){e.shotT-=dt;if(e.shotT<=0){shootEnemy(e);e.shotT=e.type==='boss'?1.05:2.1}}if(e.type==='boss'){e.burstT-=dt;if(e.burstT<=0){e.burstT=3.2;for(let i=0;i<18;i++){const ang=i/18*TAU;g.enemyBullets.push({x:e.x,y:e.y,vx:Math.cos(ang)*120,vy:Math.sin(ang)*120,r:4,damage:e.damage*.42,life:5,color:'#b48cff'})}}}if(d<e.r+p.r)hurtPlayer(e.damage,e.x,e.y,e);if(p.orbitals&&e.orbHit<=0)for(let i=0;i<p.orbitals;i++){const a2=p.orbitA+i/p.orbitals*TAU,ox=p.x+Math.cos(a2)*52,oy=p.y+Math.sin(a2)*52;if((e.x-ox)**2+(e.y-oy)**2<(e.r+7)**2){e.orbHit=.3;damageEnemy(e,p.damage*.85,false,false);break}}}
for(const b of g.bullets){b.x+=b.vx*dt;b.y+=b.vy*dt;b.life-=dt;for(const e of g.enemies){if(e.dead||b.hit.has(e))continue;if((e.x-b.x)**2+(e.y-b.y)**2<(e.r+b.r)**2){b.hit.add(e);damageEnemy(e,b.damage,true,true);applyStatuses(e);if(p.knockback){const l=Math.hypot(b.vx,b.vy)||1;e.x=clamp(e.x+b.vx/l*p.knockback,20,WORLD_W-20);e.y=clamp(e.y+b.vy/l*p.knockback,20,WORLD_H-20)}if(p.explosive&&Math.random()<p.explosive)areaDamage(e.x,e.y,72,p.damage*.55,e,'#ffbd70');if(b.pierce>0)b.pierce--;else if(b.ricochet>0){const n=nearest(e,e,190);if(n){const a=Math.atan2(n.y-e.y,n.x-e.x),sp=Math.hypot(b.vx,b.vy);b.vx=Math.cos(a)*sp;b.vy=Math.sin(a)*sp;b.ricochet--;b.x=e.x;b.y=e.y}else b.life=0}else b.life=0;if(b.life<=0)break}}}
for(const b of g.enemyBullets){b.x+=b.vx*dt;b.y+=b.vy*dt;b.life-=dt;if((p.x-b.x)**2+(p.y-b.y)**2<(p.r+b.r)**2){b.life=0;hurtPlayer(b.damage,b.x,b.y)}}for(const gem of g.gems){gem.vx*=Math.pow(.03,dt);gem.vy*=Math.pow(.03,dt);gem.x+=gem.vx*dt;gem.y+=gem.vy*dt;const d=Math.hypot(p.x-gem.x,p.y-gem.y);if(d<p.magnet){const sp=260+(p.magnet-d)