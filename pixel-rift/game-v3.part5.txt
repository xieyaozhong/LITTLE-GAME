,color,size=12,align='left'){ctx.font=`bold ${size}px Courier New`;ctx.textAlign=align;ctx.fillStyle='#000';ctx.fillText(txt,x+2,y+2);ctx.fillStyle=color;ctx.fillText(txt,x,y)}function burst(x,y,color,n=8){if(!game)return;for(let i=0;i<n;i++){const a=Math.random()*TAU,s=rand(30,130),life=rand(.2,.55);game.particles.push({type:'dot',x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,size:rand(2,5),color,life,max:life})}}function textPop(x,y,text,color){if(game)game.texts.push({x,y,text,color,life:.7})}
function updateHud(){const p=game.player;$('hpText').textContent=`${Math.ceil(Math.max(0,p.hp))} / ${Math.ceil(p.maxHp)}`;$('hpBar').style.width=clamp(p.hp/p.maxHp,0,1)*100+'%';$('levelText').textContent=p.level;$('xpBar').style.width=p.xp/p.xpNeed*100+'%';$('waveText').textContent=game.wave;const m=(game.t/60)|0,s=(game.t%60)|0;$('timeText').textContent=String(m).padStart(2,'0')+':'+String(s).padStart(2,'0');$('scoreText').textContent=Math.floor(game.score).toLocaleString();$('coinText').textContent=game.coins;$('comboText').textContent=game.combo.toFixed(1);$('dashText').textContent=p.dashT<=0?'READY':p.dashT.toFixed(1)+'s';$('fovText').textContent=FOV[save.fov].name;if(p.maxShield){$('shieldRow').style.display='block';$('shieldText').textContent=`${Math.ceil(p.shield)} / ${p.maxShield}`;$('shieldBar').style.width=clamp(p.shield/p.maxShield,0,1)*100+'%'}else $('shieldRow').style.display='none';if(game.boss&&!game.boss.dead){$('bossWrap').style.display='block';$('bossBar').style.width=clamp(game.boss.hp/game.boss.maxHp,0,1)*100+'%'}else $('bossWrap').style.display='none'}
function toast(msg){const t=$('toast');t.textContent=msg;t.classList.add('show');clearTimeout(toast.timer);toast.timer=setTimeout(()=>t.classList.remove('show'),1400)}function updateFovLabels(){$('fovBtn').textContent='視野：'+FOV[save.fov].name;$('pauseFovText').textContent=FOV[save.fov].name;$('fovText').textContent=FOV[save.fov].name}function updateMenuStats(){$('bestScore').textContent=save.bestScore.toLocaleString();$('bestWave').textContent=save.bestWave;$('bankCoins').textContent=save.coins;$('totalKills').textContent=save.totalKills.toLocaleString();$('soundBtn').textContent='音效：'+(save.sound?'開':'關');updateFovLabels()}function renderShop(){$('shopCoins').textContent=save.coins;$('shopGrid').innerHTML='';for(const d of shopDefs){const lv=save.upgrades[d.id]||0,cost=d.cost(lv),max=lv>=d.max,el=document.createElement('div');el.className='shop-item';el.innerHTML=`<h3>${d.name}</h3><p>${d.desc}</p><div class="shop-level">LV ${lv} / ${d.max}</div><button class="pixel-btn small ${max?'':'gold'}">${max?'已滿級':'◈ '+cost}</button>`;el.querySelector('button').onclick=()=>{if(max)return;if(save.coins<cost){toast('裂隙幣不足');return}save.coins-=cost;save.upgrades[d.id]=lv+1;persist();renderShop();updateMenuStats();sfx('coin')};$('shopGrid').appendChild(el)}}
function togglePause(){if(state==='playing'){state='paused';$('pauseOverlay').classList.add('show')}else if(state==='paused'){state='playing';$('pauseOverlay').classList.remove('show');last=performance.now()}}function backMenu(){state='menu';game=null;$('gameOverOverlay').classList.remove('show');$('pauseOverlay').classList.remove('show');$('menuOverlay').classList.add('show');$('hud').hidden=true;$('mobileControls').style.display='none';updateMenuStats()}
addEventListener('keydown',e=>{keys[e.key.toLowerCase()]=true;keys[e.key]=true;if([' ','ArrowUp','ArrowDown','ArrowLeft','ArrowRight'].includes(e.key))e.preventDefault();if(e.key===' ')dash();if(e.key.toLowerCase()==='v')cycleFov();if(e.key.toLowerCase()==='p'||e.key==='Escape')togglePause()},{passive:false});addEventListener('keyup',e=>{keys[e.key.toLowerCase()]=false;keys[e.key]=false});document.addEventListener('visibilitychange',()=>{if(document.hidden&&state==='playing')togglePause()});$('heroCards').addEventListener('click',e=>{const card=e.target.closest('.card');if(!card)return;selectedHero=card.dataset.hero;document.querySelectorAll('#heroCards .card').forEach(c=>c.classList.toggle('selected',c===card));sfx('coin')});$('startBtn').onclick=newGame;$('retryBtn').onclick=newGame;$('menuBtn').onclick=backMenu;$('resumeBtn').onclick=togglePause;$('pauseMobile').onclick=togglePause;$('quitBtn').onclick=()=>endGame('你主動關閉了裂隙');$('shopBtn').onclick=()=>{renderShop();$('shopOverlay').classList.add('show')};$('shopBack').onclick=()=>$('shopOverlay').classList.remove('show');$('helpBtn').onclick=()=>$('helpOverlay').classList.add('show');$('helpBack').onclick=()=>$('helpOverlay').classList.remove('show');$('soundBtn').onclick=()=>{save.sound=!save.sound;persist();updateMenuStats();if(save.sound)sfx('coin')};$('fovBtn').onclick=cycleFov;$('pauseFovBtn').onclick=cycleFov;
const dashButton=$('dashMobile');function mobileDash(e){e.preventDefault();e.stopPropagation();dash()}dashButton.addEventListener('pointerdown',mobileDash,{passive:false});dashButton.onclick=e=>e.preventDefault();const zone=$('stickZone'),knob=$('stickKnob');function stickMove(e){const r=zone.getBoundingClientRect(),cx=r.left+r.width/2,cy=r.top+r.height/2;let x=e.clientX-cx,y=e.clientY-cy,l=Math.hypot(x,y),max=r.width*.34;if(l>max){x=x/l*max;y=y/l*max}joystick.x=x/max;joystick.y=y/max;knob.style.transform=`translate(${x}px,${y}px)`}zone.addEventListener('pointerdown',e=>{joystick.active=true;joystick.pointer=e.pointerId;zone.setPointerCapture(e.pointerId);stickMove(e)});zone.addEventListener('pointermove',e=>{if(joystick.active&&e.pointerId===joystick.pointer)stickMove(e)});function stickEnd(){joystick.active=false;joystick.x=joystick.y=0;knob.style.transform=''}zone.addEventListener('pointerup',stickEnd);zone.addEventListener('pointercancel',stickEnd);
function loop(now){const dt=(now-last)/1000;last=now;if(state==='playing')update(dt);draw();requestAnimationFrame(loop)}updateMenuStats();draw();requestAnimationFrame(loop);
})();
